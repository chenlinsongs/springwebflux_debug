# 视频流式上传 - 故障排查指南

## 问题1: 使用 Multipart 方式，服务端没有立即创建文件

### 症状

```
iOS显示上传进度: 0% → 50% → 100%
服务端磁盘: 无文件
          ↓
iOS显示完成
服务端: 才开始创建文件并写入所有数据
```

### 根本原因

**这不是bug！这是 HTTP multipart/form-data 协议的设计限制！**

```java
// Spring WebFlux Multipart 处理流程
1. 客户端上传数据
   ↓
2. Spring 接收数据并写入临时文件（这部分是流式的）
   ↓
3. 解析 multipart boundary，提取元数据
   ↓
4. 创建 FilePart 对象
   ↓
5. Mono<FilePart> 发出信号 ← 您的代码才开始执行！
   ↓
6. 从临时文件复制到目标位置
```

**关键点**：
- 步骤2是流式的（不会OOM）
- 但步骤5要等步骤1-4完成
- 这是协议限制，无法避免

### 解决方案

#### 方案A: 接受限制 + 优化（推荐给iOS）

使用 `/upload-video-ios-optimized` + 配置优化

```yaml
# application.yml
spring:
  webflux:
    multipart:
      file-storage-directory: /ssd/temp  # 使用SSD
  codec:
    max-in-memory-size: 10MB
```

**效果**：
- 性能提升30-40%
- 无需修改iOS代码
- 仍需等待完整上传（这是协议限制）

#### 方案B: 放弃 Multipart（真正流式）

使用 `/upload-video-raw`

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4
```

**效果**：
- ✅ 真正的流式上传
- ✅ 数据到达立即写入
- ❌ 需要修改iOS代码

### 验证

**如果使用方案A（Multipart优化）**：

```bash
# 上传并观察日志
curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@test.mp4' \
  --limit-rate 5M

# 应该看到：
# ⏰ 时间统计:
#   总耗时: 12.00 秒
#   上传等待: 10.00 秒  ← 这是正常的！
#   复制耗时: 2.00 秒
```

**如果使用方案B（Raw Binary）**：

```bash
# 上传并观察日志
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test.mp4 \
  --limit-rate 5M

# 应该看到：
# 💾 视频已刷新到磁盘 | 数据块: 500 | 磁盘大小: 2.50 MB | 速度: 5.0 MB/s
# ← 上传开始后50ms内就看到这条日志！
```

## 问题2: 看不到"视频已刷新到磁盘"日志

### 症状

使用 `/upload-video-streaming` 或 `/upload-video-raw`，但没有看到预期的日志输出。

### 可能原因

#### 原因1: 使用了错误的接口

```bash
# ❌ 错误 - 这个接口不会打印实时日志
curl -X POST http://localhost:8080/image/upload-video \
  -F 'file=@video.mp4'

# ✅ 正确 - Raw Binary方式会打印实时日志
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4
```

#### 原因2: forceInterval 为 null 或 0

```bash
# ❌ 不会打印中间日志
-H "X-Force-Interval: 0"

# ✅ 会每500个数据块打印一次
-H "X-Force-Interval: 500"
```

#### 原因3: 日志级别不正确

检查 `application.yml`：

```yaml
logging:
  level:
    org.example.springwebflux.controller: DEBUG  # 或 INFO
```

#### 原因4: 文件太小

如果文件只有几MB，可能上传太快，来不及看到中间日志。

**解决**：
```bash
# 创建大文件测试
dd if=/dev/zero of=large.mp4 bs=1m count=100

# 限速上传，方便观察
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: large.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @large.mp4 \
  --limit-rate 5M  # 限速5MB/s
```

### 验证

```bash
# 确认使用正确的接口和参数
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test.mp4 \
  --limit-rate 5M \
  -v  # 启用详细输出

# 同时在另一个终端查看日志
tail -f logs/spring.log | grep "视频"
```

## 问题3: 使用 Multipart，但希望看到实时进度

### 症状

使用 `/upload-video-streaming`，但仍然是等待完整上传后才处理。

### 根本原因

**`Mono<FilePart>` 本质上无法实现真正的流式处理！**

即使您的代码中使用了 `AsynchronousFileChannel`，但 `Mono<FilePart>` 的触发时机就已经是在完整上传之后了。

### 流程对比

#### 您认为的流程（错误）

```
客户端上传
   ↓ (实时)
Mono<FilePart>
   ↓ (实时)
AsynchronousFileChannel.write()  ← 以为这里是实时的
```

#### 实际流程（正确）

```
客户端上传
   ↓ (等待完整上传)
临时文件写入完成
   ↓
解析multipart
   ↓
Mono<FilePart> 触发  ← 这里已经上传完了！
   ↓
AsynchronousFileChannel.write()  ← 这里只是从临时文件复制
```

### 解决方案

**唯一的真正流式方案**：使用 `/upload-video-raw`

不使用 Multipart，直接消费 `Flux<DataBuffer>`：

```java
@PostMapping(value = "/upload-video-raw", 
             consumes = MediaType.APPLICATION_OCTET_STREAM_VALUE)
public Mono<ResponseEntity<Map<String, Object>>> uploadVideoRaw(
        @RequestHeader("X-Filename") String filename,
        @RequestBody Flux<DataBuffer> dataBufferFlux) {  // ← 关键！
    
    return Mono.using(
        () -> AsynchronousFileChannel.open(...),
        channel -> dataBufferFlux
            .doOnNext(buffer -> {
                // 这里是真正实时的！
                logger.info("✅ 实时接收: {} bytes", buffer.readableByteCount());
            })
            .concatMap(buffer -> writeToChannel(channel, buffer, position))
            .then(),
        channel -> channel.close()
    );
}
```

## 问题4: 磁盘空间不足

### 症状

```
IOException: No space left on device
```

### 原因

Multipart 方式会使用临时文件：
- 原文件: 500MB
- 临时文件: 500MB
- 目标文件: 500MB

**总共需要**: 1.5GB

### 解决方案

#### 方案1: 清理临时文件

```bash
# 查看临时文件
ls -lh /tmp/spring-multipart-*

# 清理
rm /tmp/spring-multipart-*
```

#### 方案2: 配置临时文件位置

```yaml
spring:
  webflux:
    multipart:
      file-storage-directory: /path/to/large/disk/temp
```

#### 方案3: 使用 Raw Binary（不产生临时文件）

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: video.mp4" \
  --data-binary @video.mp4
```

## 问题5: 上传速度很慢

### 诊断

查看日志中的时间统计：

```log
⏰ 时间统计:
  总耗时: 35.00 秒
  上传等待: 30.00 秒  ← 网络上传时间（无法优化）
  复制耗时: 5.00 秒   ← 可以优化！
  复制占比: 14.3%
```

### 优化方案

#### 如果"复制耗时"很长（>10%）

**优化策略**：

1. **使用SSD存储**：

```yaml
spring:
  webflux:
    multipart:
      file-storage-directory: /ssd/temp
```

2. **使用优化接口**：

```bash
# 使用 transferTo 进行系统级优化
curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@video.mp4'
```

3. **目标位置也使用SSD**：

```java
String videoDirPath = "/ssd/videos/";  // 改为SSD路径
```

#### 如果"上传等待"很长（>90%）

**这是网络问题，服务端无法优化**

可以考虑：
- 压缩视频
- 使用更快的网络
- 分片上传

## 快速诊断清单

### 检查1: 确认接口类型

```bash
# Multipart - 会等待完整上传
/upload-video
/upload-video-streaming
/upload-video-ios-optimized

# Raw Binary - 真正流式
/upload-video-raw
```

### 检查2: 确认请求格式

```bash
# Multipart
-F 'file=@video.mp4'

# Raw Binary
-H "Content-Type: application/octet-stream" \
-H "X-Filename: video.mp4" \
--data-binary @video.mp4
```

### 检查3: 确认日志配置

```yaml
logging:
  level:
    org.example.springwebflux.controller: DEBUG
```

### 检查4: 确认文件大小和限速

```bash
# 创建测试文件
dd if=/dev/zero of=test.mp4 bs=1m count=50

# 限速上传
--limit-rate 5M
```

### 检查5: 查看实时日志

```bash
tail -f logs/spring.log | grep "视频"
```

## 总结

| 问题 | 根本原因 | 推荐方案 |
|-----|---------|---------|
| 等待完整上传 | Multipart协议限制 | 使用Raw Binary或接受限制 |
| 看不到实时日志 | 接口类型或参数错误 | 检查接口和forceInterval |
| 磁盘空间不足 | 临时文件占用 | 配置临时目录或用Raw Binary |
| 上传很慢 | 磁盘I/O慢 | 使用SSD + 优化接口 |

**核心认识**：
- Multipart **不是真正的流式**
- 要真正流式，必须用 `/upload-video-raw`
- 这是协议限制，不是实现问题

需要更多帮助，请查看：
- `视频上传方案总结.md`
- `WebFlux_Multipart限制说明.md`
- `真正的流式上传-立即测试.md`

