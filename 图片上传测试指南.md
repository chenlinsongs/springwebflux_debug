# 图片上传测试指南

## 快速开始

### 1. 单图上传

```bash
curl -X POST http://localhost:8080/image/upload-single \
  -F 'file=@image.jpg'
```

### 2. 批量上传

```bash
curl -X POST http://localhost:8080/image/upload-batch \
  -F 'files=@image1.jpg' \
  -F 'files=@image2.jpg' \
  -F 'files=@image3.jpg'
```

### 3. 视频上传（标准）

```bash
curl -X POST http://localhost:8080/image/upload-video \
  -F 'file=@video.mp4'
```

### 4. 视频上传（iOS优化 - 推荐）⭐

```bash
curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@video.mp4'
```

### 5. 视频上传（真正流式 - 需改客户端）

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4
```

### 6. Live Photo上传

```bash
curl -X POST http://localhost:8080/image/upload-live-photo \
  -F 'image=@IMG_1234.HEIC' \
  -F 'video=@IMG_1234.MOV'
```

### 7. 文件存在性检查

```bash
curl -X POST http://localhost:8080/image/check-exists \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "test.jpg",
    "fileSize": 1024000,
    "md5": "abc123..."
  }'
```

## 创建测试文件

```bash
# 创建小图片 (1MB)
dd if=/dev/zero of=small.jpg bs=1m count=1

# 创建大图片 (10MB)
dd if=/dev/zero of=large.jpg bs=1m count=10

# 创建视频 (50MB)
dd if=/dev/zero of=video.mp4 bs=1m count=50

# 创建大视频 (500MB)
dd if=/dev/zero of=large_video.mp4 bs=1m count=500
```

## 性能测试

### 测试Multipart上传（优化版）

```bash
time curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@large_video.mp4'
```

### 测试Raw Binary上传

```bash
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: large_video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @large_video.mp4
```

## 查看日志

```bash
tail -f logs/spring.log | grep "视频"
```

## 检查上传目录

```bash
# 查看图片目录
ls -lh ~/Pictures/uploads/

# 查看视频目录
ls -lh ~/Movies/uploads/

# 查看临时文件
ls -lh /tmp/spring-multipart-*
```

## iOS Swift 示例

### 标准Multipart上传

```swift
func uploadVideo(url: URL) {
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-ios-optimized")!)
    request.httpMethod = "POST"
    
    let boundary = UUID().uuidString
    request.setValue("multipart/form-data; boundary=\(boundary)", forHTTPHeaderField: "Content-Type")
    
    let data = try! Data(contentsOf: url)
    var body = Data()
    
    body.append("--\(boundary)\r\n")
    body.append("Content-Disposition: form-data; name=\"file\"; filename=\"video.mp4\"\r\n")
    body.append("Content-Type: video/mp4\r\n\r\n")
    body.append(data)
    body.append("\r\n--\(boundary)--\r\n")
    
    request.httpBody = body
    
    URLSession.shared.dataTask(with: request) { data, response, error in
        // 处理响应
    }.resume()
}
```

### Raw Binary上传（真正流式）

```swift
func uploadVideoRaw(url: URL) {
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-raw")!)
    request.httpMethod = "POST"
    request.setValue("application/octet-stream", forHTTPHeaderField: "Content-Type")
    request.setValue("video.mp4", forHTTPHeaderField: "X-Filename")
    request.setValue("500", forHTTPHeaderField: "X-Force-Interval")
    
    request.httpBodyStream = InputStream(url: url)
    
    URLSession.shared.uploadTask(with: request, fromFile: url) { data, response, error in
        // 处理响应
    }.resume()
}
```

## 接口对比

| 接口 | Multipart | 真正流式 | iOS兼容 | 推荐 |
|-----|----------|---------|---------|------|
| `/upload-video` | ✅ | ❌ | ✅ | 小文件 |
| `/upload-video-ios-optimized` | ✅ | ❌ | ✅ | **iOS推荐** ⭐ |
| `/upload-video-raw` | ❌ | ✅ | ❌ | 可改客户端 |

## 故障排查

### 问题1: 上传后服务端没有立即创建文件

**原因**: 使用Multipart方式，需要等待完整上传

**解决**: 
- 这是正常现象
- 查看 `WebFlux_Multipart限制说明.md`
- 或使用 `/upload-video-raw` 接口

### 问题2: 上传大文件很慢

**优化**:
1. 配置SSD临时目录
2. 增大内存缓冲区
3. 使用 `/upload-video-ios-optimized`

### 问题3: 磁盘空间不足

**原因**: Multipart会使用临时文件

**解决**:
- 清理 /tmp 目录
- 配置临时文件位置到大空间磁盘

## 下一步

查看以下文档了解更多：
- `视频上传方案总结.md` - 方案完整对比
- `WebFlux_Multipart限制说明.md` - 深入理解限制
- `文件上传完整文档索引.md` - 所有文档索引

