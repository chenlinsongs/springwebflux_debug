# 等待外部接口 - 测试指南

## 📋 快速开始

### 1. 启动应用

```bash
# 运行 Spring Boot 应用
mvn spring-boot:run

# 或者直接运行 Main 类
```

### 2. 测试各个场景

所有接口的前缀：`http://localhost:8080/wait-external`

---

## 🎯 基础场景测试

### 场景1：最简单的调用

```bash
# 直接返回外部接口的 Mono
curl http://localhost:8080/wait-external/simple

# 响应示例：
# 来自外部接口的数据：mock-service-data-12345

# 控制台输出：
# === 场景1：直接返回外部接口的 Mono ===
# 1. Controller 方法被调用
# 2. 框架 subscribe，发起 HTTP 请求
# 3. 收到外部接口响应：mock-service-data-12345
```

### 场景2：处理外部数据

```bash
# 调用外部接口并处理结果
curl http://localhost:8080/wait-external/with-processing

# 响应示例：
# {
#   "original": "mock-service-data-12345",
#   "processed": "MOCK-SERVICE-DATA-12345",
#   "timestamp": 1234567890
# }
```

---

## 🔄 串行 vs 并行对比

### 场景3：串行调用（慢）

```bash
# 串行调用3个接口
curl http://localhost:8080/wait-external/sequential

# 响应示例：
# 串行调用完成：data1 → data2 → data3
# 总耗时：600ms（约600ms）

# 说明：3个接口依次调用，总时间 = 200 + 200 + 200 = 600ms
```

### 场景4：并行调用（快） ⭐

```bash
# 并行调用3个接口
curl http://localhost:8080/wait-external/parallel

# 响应示例：
# 并行调用完成：[data1, data2, data3]
# 总耗时：200ms（约200ms）
# 节省时间：400ms（66%）

# 说明：3个接口同时调用，总时间 = max(200, 200, 200) = 200ms
```

**对比**：
- 串行：600ms
- 并行：200ms
- **节省：66% 的时间！**

---

## ⚙️ 高级功能测试

### 场景5：超时处理

```bash
# 带超时和错误处理
curl http://localhost:8080/wait-external/with-timeout

# 正常情况：
# mock-service-data-12345

# 超时情况（如果服务慢）：
# 默认值（服务不可用）
```

### 场景6：条件调用

```bash
# VIP 用户（额外调用）
curl http://localhost:8080/wait-external/conditional/vip

# 响应示例：
# data + VIP额外数据(extra-data)

# 普通用户（不额外调用）
curl http://localhost:8080/wait-external/conditional/normal

# 响应示例：
# data
```

### 场景7：重试机制

```bash
# 失败时重试3次
curl http://localhost:8080/wait-external/with-retry

# 控制台输出（如果失败）：
# 重试第0次
# 重试第1次
# 重试第2次
# 重试3次后仍失败
```

### 场景8：Mono.defer

```bash
# 延迟创建 Mono
curl http://localhost:8080/wait-external/with-defer

# 控制台输出：
# 1. Controller 方法被调用
# 2. Mono.defer 被执行（subscribe时）
```

### 场景9：复杂业务流程

```bash
# 模拟真实业务：获取用户 → 并行获取订单和地址 → 获取支付信息
curl http://localhost:8080/wait-external/complex-flow

# 响应示例：
# ComplexResult{
#   user='user-data', 
#   orders='orders-data', 
#   address='address-data', 
#   payment='payment-data'
# }

# 控制台输出：
# 步骤1：获取用户 = user-data
# 步骤2：并行获取订单和地址
#   订单数据：orders-data
#   地址数据：address-data
# 步骤3：获取支付信息
# 完成：ComplexResult{...}
```

---

## ❌ 错误示范 vs ✅ 正确做法

### 错误：使用 block()

```bash
curl http://localhost:8080/wait-external/wrong-with-block

# 响应：
# ❌ 错误做法：使用了 block()，线程被阻塞
# 结果：mock-service-data-12345

# 控制台输出：
# 调用 block()，线程阻塞...
# block() 返回，线程释放

# 问题：
# - 线程被阻塞 200ms
# - 破坏异步模型
# - 降低并发能力
```

### 正确：直接返回 Mono

```bash
curl http://localhost:8080/wait-external/correct-no-block

# 响应：
# ✅ 正确做法：直接返回 Mono
# 结果：mock-service-data-12345

# 优点：
# - 线程不阻塞
# - 保持异步模型
# - 高并发能力
```

---

## 📊 总结接口

```bash
# 查看所有场景的总结
curl http://localhost:8080/wait-external/summary

# 响应：
# === 等待外部接口返回 Mono 总结 ===
# 
# 核心要点：
# 1. 直接返回 WebClient 的 Mono
# 2. 框架会自动等待外部接口响应
# 3. 不需要手动等待，不要使用 block()
# 
# ... 更多内容
```

---

## 🧪 压力测试

### 测试串行 vs 并行的性能差异

```bash
# 准备工作
brew install apache-bench  # macOS
# 或
sudo apt install apache2-utils  # Linux

# 测试串行接口（慢）
ab -n 100 -c 10 http://localhost:8080/wait-external/sequential

# 测试并行接口（快）
ab -n 100 -c 10 http://localhost:8080/wait-external/parallel

# 对比结果：
# 串行：约 60 秒（100 * 600ms / 10）
# 并行：约 20 秒（100 * 200ms / 10）
# 并行快 3 倍！
```

---

## 💡 学习路径

### 入门（必看）

1. **场景1** - `/simple`
   - 理解基本调用方式
   
2. **场景3 vs 场景4** - `/sequential` vs `/parallel`
   - 理解串行和并行的区别
   - 体会性能差异

3. **错误 vs 正确** - `/wrong-with-block` vs `/correct-no-block`
   - 理解为什么不能用 block()

### 进阶

4. **场景5** - `/with-timeout`
   - 学习超时处理
   
5. **场景6** - `/conditional/{type}`
   - 学习条件调用
   
6. **场景7** - `/with-retry`
   - 学习重试机制

### 高级

7. **场景9** - `/complex-flow`
   - 学习复杂业务流程
   - 组合串行和并行调用

---

## 🎯 核心概念验证

### 验证1：框架自动等待

```bash
# 启动应用，观察控制台
curl http://localhost:8080/wait-external/simple

# 控制台会显示：
# 1. Controller 方法被调用
# 2. 框架 subscribe，发起 HTTP 请求
# 3. 收到外部接口响应：...

# 证明：框架确实会 subscribe 并等待响应
```

### 验证2：线程不阻塞

```bash
# 同时发起多个请求
for i in {1..5}; do
  curl http://localhost:8080/wait-external/simple &
done
wait

# 观察：5个请求几乎同时完成
# 证明：线程没有阻塞，可以同时处理多个请求
```

### 验证3：并行确实更快

```bash
# 记录时间
time curl http://localhost:8080/wait-external/sequential
# 约 0.6 秒

time curl http://localhost:8080/wait-external/parallel
# 约 0.2 秒

# 证明：并行确实比串行快 3 倍
```

---

## 🐛 常见问题排查

### 问题1：连接超时

```
症状：请求超过5秒没响应

原因：WebClientConfig 中设置了5秒超时

解决：
- 增加超时时间
- 检查目标服务是否可用
```

### 问题2：找不到 WebClient Bean

```
症状：启动报错 "No qualifying bean of type 'WebClient'"

原因：WebClientConfig 没有被扫描

解决：
- 确保 WebClientConfig 在主应用包下
- 或添加 @ComponentScan
```

### 问题3：响应格式不对

```
症状：bodyToMono(String.class) 报错

原因：目标服务返回的不是文本

解决：
- 改用 bodyToMono(YourClass.class)
- 或用 bodyToMono(new ParameterizedTypeReference<...>() {})
```

---

## 📝 实际应用建议

### 1. 何时用串行

```
场景：第2个请求依赖第1个请求的结果

示例：
1. 获取用户ID
2. 用用户ID查询订单

代码：
return getUserId()
    .flatMap(id -> getOrders(id))
```

### 2. 何时用并行

```
场景：多个请求互不依赖

示例：
1. 获取用户信息
2. 获取系统配置
3. 获取推荐列表

代码：
return Mono.zip(getUser(), getConfig(), getRecommend())
    .map(tuple -> combine(...))
```

### 3. 何时用条件调用

```
场景：根据条件决定是否调用某接口

示例：
- VIP用户显示额外信息
- 有优惠券时查询优惠详情

代码：
return getUser()
    .flatMap(user -> {
        if (user.isVip()) {
            return getVipInfo(user);
        } else {
            return Mono.just(user);
        }
    })
```

### 4. 何时用重试

```
场景：网络不稳定，临时故障

示例：
- 调用第三方API
- 微服务间调用

代码：
return webClient.get()...
    .retryWhen(Retry.backoff(3, Duration.ofSeconds(1)))
    .onErrorResume(e -> Mono.just(defaultValue))
```

---

## ✅ 自检清单

完成测试后，请确认你理解了：

- [ ] 如何直接返回外部接口的 Mono
- [ ] 为什么不需要手动等待
- [ ] 为什么不能用 block()
- [ ] 串行调用的实现（flatMap）
- [ ] 并行调用的实现（Mono.zip）
- [ ] 串行 vs 并行的性能差异
- [ ] 如何处理超时
- [ ] 如何条件调用
- [ ] 如何实现重试
- [ ] 如何组合复杂流程

---

## 🎓 记忆口诀

```
外部接口返回 Mono，
直接返回不用等。

框架自动会 subscribe，
异步等待它负责。

串行用 flatMap 链，
并行用 Mono.zip。

千万不要用 block，
破坏异步是大忌。

超时重试错误处理，
保证服务稳定性。
```

---

现在你可以开始测试了！🚀

建议顺序：
1. `/simple` - 理解基础
2. `/sequential` vs `/parallel` - 体会差异  
3. `/wrong-with-block` vs `/correct-no-block` - 避免错误
4. 其他高级场景 - 深入学习

