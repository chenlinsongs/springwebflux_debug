# 文件上传接口快速参考

## 所有可用接口一览

### 图片上传

#### 1. 单图上传
```bash
POST /image/upload-single
Content-Type: multipart/form-data
Body: file=<图片文件>
```

#### 2. 批量上传
```bash
POST /image/upload-batch
Content-Type: multipart/form-data
Body: files=<图片1>, files=<图片2>, ...
```

### 视频上传

#### 3. 标准视频上传
```bash
POST /image/upload-video
Content-Type: multipart/form-data
Body: file=<视频文件>
```

特点：
- ✅ 简单易用
- ❌ 等待完整上传

#### 4. 优化版视频上传（iOS推荐）⭐
```bash
POST /image/upload-video-ios-optimized
Content-Type: multipart/form-data
Body: file=<视频文件>
```

特点：
- ✅ iOS完全兼容
- ✅ 使用transferTo优化
- ✅ 提供详细性能统计
- ❌ 仍需等待完整上传（协议限制）

#### 5. Raw Binary上传（真正流式）🚀
```bash
POST /image/upload-video-raw
Content-Type: application/octet-stream
Headers:
  X-Filename: video.mp4
  X-Content-Type: video/mp4 (可选)
  X-Force-Interval: 500 (可选)
Body: <二进制数据>
```

特点：
- ✅ 真正的流式上传
- ✅ 数据到达立即写入
- ✅ 无临时文件
- ❌ 需要修改客户端代码

### Live Photo上传

#### 6. Live Photo上传
```bash
POST /image/upload-live-photo
Content-Type: multipart/form-data
Body: 
  image=<HEIC文件>
  video=<MOV文件>
```

### 文件检查

#### 7. 文件存在性检查
```bash
POST /image/check-exists
Content-Type: application/json
Body: {
  "filename": "test.jpg",
  "fileSize": 1024000,
  "md5": "abc123..."
}
```

## curl 命令示例

### 单图上传
```bash
curl -X POST http://localhost:8080/image/upload-single \
  -F 'file=@photo.jpg'
```

### 批量上传
```bash
curl -X POST http://localhost:8080/image/upload-batch \
  -F 'files=@photo1.jpg' \
  -F 'files=@photo2.jpg' \
  -F 'files=@photo3.jpg'
```

### 视频上传（iOS优化版）
```bash
curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@video.mp4'
```

### 视频上传（真正流式）
```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4
```

### Live Photo上传
```bash
curl -X POST http://localhost:8080/image/upload-live-photo \
  -F 'image=@IMG_1234.HEIC' \
  -F 'video=@IMG_1234.MOV'
```

### 文件存在性检查
```bash
curl -X POST http://localhost:8080/image/check-exists \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "test.jpg",
    "fileSize": 1024000,
    "md5": "abc123def456"
  }'
```

## iOS Swift 代码示例

### Multipart上传（推荐）
```swift
func uploadVideo(url: URL) {
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-ios-optimized")!)
    request.httpMethod = "POST"
    
    let boundary = UUID().uuidString
    request.setValue("multipart/form-data; boundary=\(boundary)", forHTTPHeaderField: "Content-Type")
    
    // 构建multipart body
    let data = try! Data(contentsOf: url)
    var body = Data()
    body.append("--\(boundary)\r\n")
    body.append("Content-Disposition: form-data; name=\"file\"; filename=\"video.mp4\"\r\n")
    body.append("Content-Type: video/mp4\r\n\r\n")
    body.append(data)
    body.append("\r\n--\(boundary)--\r\n")
    
    request.httpBody = body
    
    URLSession.shared.dataTask(with: request).resume()
}
```

### Raw Binary上传（真正流式）
```swift
func uploadVideoRaw(url: URL) {
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-raw")!)
    request.httpMethod = "POST"
    request.setValue("application/octet-stream", forHTTPHeaderField: "Content-Type")
    request.setValue("video.mp4", forHTTPHeaderField: "X-Filename")
    request.setValue("500", forHTTPHeaderField: "X-Force-Interval")
    
    request.httpBodyStream = InputStream(url: url)
    
    URLSession.shared.uploadTask(with: request, fromFile: url).resume()
}
```

## 接口选择决策树

```
需要上传视频？
  ├─ 是
  │   ├─ 可以修改客户端代码？
  │   │   ├─ 是 → 使用 /upload-video-raw （真正流式）
  │   │   └─ 否 → 使用 /upload-video-ios-optimized （iOS推荐）
  │   └─ 文件很小（<50MB）？
  │       └─ 是 → 使用 /upload-video （简单方案）
  └─ 否
      ├─ Live Photo？
      │   └─ 是 → 使用 /upload-live-photo
      ├─ 多个图片？
      │   └─ 是 → 使用 /upload-batch
      └─ 单个图片？
          └─ 是 → 使用 /upload-single
```

## 性能对比（1.5GB视频）

| 接口 | 总时间 | 临时文件 | 真正流式 | iOS兼容 |
|-----|--------|---------|---------|---------|
| `/upload-video` | 35秒 | 是 | ❌ | ✅ |
| `/upload-video-ios-optimized` | 22秒 | 是 | ❌ | ✅ |
| `/upload-video-raw` | 20秒 | 否 | ✅ | ❌ |

## 响应格式

### 成功响应
```json
{
  "filename": "video_20231122_123456.mp4",
  "size": 1572864000,
  "path": "/Users/user/Movies/uploads/video_20231122_123456.mp4",
  "uploadTime": "2023-11-22 12:34:56",
  "message": "视频上传成功",
  "timingMs": 22000
}
```

### 错误响应
```json
{
  "message": "视频上传失败: 文件类型不支持",
  "error": "IllegalArgumentException"
}
```

## 配置优化

```yaml
# application.yml
spring:
  webflux:
    multipart:
      file-storage-directory: /ssd/temp  # 使用SSD
      max-file-size: -1
      max-request-size: -1
  codec:
    max-in-memory-size: 10MB

logging:
  level:
    org.example.springwebflux.controller: DEBUG
```

## 常见问题

### Q: 为什么使用Multipart时服务端没有立即创建文件？

A: 这是HTTP multipart协议的设计限制，Spring需要等待完整上传后才能处理。查看 `WebFlux_Multipart限制说明.md`

### Q: 如何实现真正的流式上传？

A: 使用 `/upload-video-raw` 接口，但需要修改客户端代码。

### Q: iOS标准上传如何优化性能？

A: 使用 `/upload-video-ios-optimized` + SSD配置，可提升30-40%性能。

### Q: 如何选择 forceInterval 参数？

A: 
- `0`: 不强制刷新（最快，但风险高）
- `100`: 高频刷新（最安全）
- `500`: 推荐值（平衡）
- `1000`: 低频刷新（性能优先）

## 更多文档

- `视频上传方案总结.md` - 所有方案完整对比
- `WebFlux_Multipart限制说明.md` - 深入理解限制
- `真正的流式上传-立即测试.md` - 测试流式上传
- `图片上传测试指南.md` - 完整测试指南
- `视频流式上传-故障排查指南.md` - 故障排查

