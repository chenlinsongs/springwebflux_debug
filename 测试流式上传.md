# 测试流式上传

## 快速测试脚本

### 1. 创建测试文件

```bash
# 创建50MB测试文件
dd if=/dev/zero of=test_video.mp4 bs=1m count=50

# 创建500MB测试文件
dd if=/dev/zero of=large_video.mp4 bs=1m count=500

# 创建1.5GB测试文件
dd if=/dev/zero of=huge_video.mp4 bs=1m count=1500
```

### 2. 测试Multipart上传（iOS优化版）

```bash
time curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@test_video.mp4' \
  --limit-rate 5M \
  -v
```

**期望看到**：
```log
⏰ 时间统计:
  总耗时: 10.50 秒
  上传等待: 10.00 秒
  复制耗时: 0.50 秒
  复制占比: 4.8%
```

### 3. 测试Raw Binary上传（真正流式）

```bash
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test_video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test_video.mp4 \
  --limit-rate 5M \
  -v
```

**期望看到**：
```log
💾 视频已刷新到磁盘 | 数据块: 500 | 磁盘大小: 2.50 MB | 速度: 5.0 MB/s
💾 视频已刷新到磁盘 | 数据块: 1000 | 磁盘大小: 5.00 MB | 速度: 5.0 MB/s
...
✅ 视频最终刷新完成，总数据块: 2500
```

## 对比测试

### 并行测试脚本

```bash
#!/bin/bash

echo "=== 测试 Multipart 上传 ==="
time curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@test_video.mp4' \
  --limit-rate 5M

echo ""
echo "=== 测试 Raw Binary 上传 ==="
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test_video_raw.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test_video.mp4 \
  --limit-rate 5M

echo ""
echo "测试完成！"
```

### 监控磁盘写入

```bash
# 终端1: 启动上传
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test_video.mp4 \
  --limit-rate 5M

# 终端2: 监控文件大小变化
while true; do 
  ls -lh ~/Movies/uploads/test.mp4 2>/dev/null || echo "文件还未创建"
  sleep 1
done
```

**期望看到（Raw Binary）**：
```
文件还未创建
-rw-r--r--  1 user  staff   2.5M Nov 22 20:30 test.mp4
-rw-r--r--  1 user  staff   5.0M Nov 22 20:31 test.mp4
-rw-r--r--  1 user  staff   7.5M Nov 22 20:32 test.mp4
...
```

## 查看日志

```bash
# 实时查看日志
tail -f logs/spring.log | grep "视频"

# 或者
tail -f logs/spring.log | grep -E "视频|FilePart|数据块"
```

## 清理测试文件

```bash
# 清理测试文件
rm test_video.mp4 large_video.mp4 huge_video.mp4

# 清理上传的文件
rm ~/Movies/uploads/test*.mp4
rm ~/Pictures/uploads/*.jpg

# 清理临时文件
rm /tmp/spring-multipart-*
```

## 完整测试流程

```bash
#!/bin/bash

echo "🧪 开始文件上传测试"
echo ""

# 1. 创建测试文件
echo "1️⃣ 创建测试文件..."
dd if=/dev/zero of=test.mp4 bs=1m count=50 2>/dev/null
echo "✅ 已创建 50MB 测试文件"
echo ""

# 2. 测试 Multipart
echo "2️⃣ 测试 Multipart 上传..."
start_time=$(date +%s)
curl -s -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@test.mp4' \
  --limit-rate 5M > /dev/null
end_time=$(date +%s)
multipart_duration=$((end_time - start_time))
echo "✅ Multipart 上传完成，耗时: ${multipart_duration}秒"
echo ""

# 3. 测试 Raw Binary
echo "3️⃣ 测试 Raw Binary 上传..."
start_time=$(date +%s)
curl -s -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test_raw.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test.mp4 \
  --limit-rate 5M > /dev/null
end_time=$(date +%s)
raw_duration=$((end_time - start_time))
echo "✅ Raw Binary 上传完成，耗时: ${raw_duration}秒"
echo ""

# 4. 对比结果
echo "📊 性能对比:"
echo "  Multipart:   ${multipart_duration}秒"
echo "  Raw Binary:  ${raw_duration}秒"
if [ $raw_duration -lt $multipart_duration ]; then
  saved=$((multipart_duration - raw_duration))
  percent=$((saved * 100 / multipart_duration))
  echo "  Raw Binary 更快 ${saved}秒 (${percent}%)"
else
  echo "  性能相近"
fi
echo ""

# 5. 清理
echo "5️⃣ 清理测试文件..."
rm test.mp4
echo "✅ 清理完成"
echo ""

echo "🎉 测试完成！"
```

保存为 `test_upload.sh`，然后运行：

```bash
chmod +x test_upload.sh
./test_upload.sh
```

## 压力测试

### 并发上传测试

```bash
#!/bin/bash

echo "🧪 并发上传压力测试"

# 创建测试文件
dd if=/dev/zero of=test.mp4 bs=1m count=10 2>/dev/null

# 并发10个上传
for i in {1..10}; do
  {
    echo "上传 #$i 开始..."
    curl -s -X POST http://localhost:8080/image/upload-video-raw \
      -H "Content-Type: application/octet-stream" \
      -H "X-Filename: test_${i}.mp4" \
      --data-binary @test.mp4 > /dev/null
    echo "上传 #$i 完成"
  } &
done

wait
echo "✅ 所有上传完成"

# 清理
rm test.mp4
```

### 监控系统资源

```bash
# 终端1: 监控CPU和内存
top -pid $(pgrep -f spring)

# 终端2: 监控网络
nettop -p $(pgrep -f spring)

# 终端3: 监控磁盘I/O
iostat -w 1

# 终端4: 执行上传测试
./test_upload.sh
```

## 成功标准

### Multipart上传

- ✅ 上传完成后返回成功响应
- ✅ 文件正确保存到目标目录
- ✅ 日志显示详细的时间统计
- ✅ 临时文件被正确清理

### Raw Binary上传

- ✅ 上传开始后50ms内看到第一条日志
- ✅ 文件立即创建
- ✅ 文件大小持续增长
- ✅ 日志显示实时进度和速度
- ✅ 内存占用保持稳定

## 故障排查

### 测试失败

```bash
# 检查服务是否运行
curl http://localhost:8080/actuator/health

# 检查端口
lsof -i :8080

# 查看错误日志
tail -50 logs/spring.log
```

### 文件未创建

```bash
# 检查目录权限
ls -ld ~/Movies/uploads/

# 手动创建目录
mkdir -p ~/Movies/uploads/
mkdir -p ~/Pictures/uploads/

# 检查磁盘空间
df -h
```

## 下一步

- 成功 → 查看 `文件上传完整解决方案总结.md`
- 问题 → 查看 `视频流式上传-故障排查指南.md`
- 优化 → 查看 `视频上传方案总结.md`

