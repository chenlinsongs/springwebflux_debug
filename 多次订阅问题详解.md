# 多次订阅问题详解

## 🎯 你的问题

> "如果我创建了一个 mono，并自己调用了 subscribe，我再返回 mono，会发生什么？"

---

## 💥 答案：会执行两次！

```java
@GetMapping("/user")
public Mono<User> getUser() {
    Mono<User> mono = userRepository.findById(1);
    
    // 你手动 subscribe 一次
    mono.subscribe(user -> {
        System.out.println("手动 subscribe：" + user);
    });
    
    // 返回给框架，框架又 subscribe 一次
    return mono;
    
    // 结果：数据库会被查询两次！
}
```

---

## 🔍 详细分析

### 会发生什么？

```
1. 创建 Mono
   Mono<User> mono = userRepository.findById(1);
   
2. 你手动 subscribe（第1次）
   mono.subscribe(...);
   ↓
   触发第1次执行
   ↓
   查询数据库（第1次）
   ↓
   打印日志
   
3. 返回 Mono 给框架
   return mono;
   
4. 框架自动 subscribe（第2次）
   mono.subscribe(...);
   ↓
   触发第2次执行
   ↓
   查询数据库（第2次）
   ↓
   写入 HTTP 响应

结果：数据库被查询了2次！
```

---

## 📊 核心原理

### Mono 可以被多次 subscribe

```java
Mono<String> mono = Mono.fromCallable(() -> {
    System.out.println("执行了！");
    return "结果";
});

// 第1次 subscribe
mono.subscribe(result -> System.out.println("第1次：" + result));
// 输出：
// 执行了！
// 第1次：结果

// 第2次 subscribe
mono.subscribe(result -> System.out.println("第2次：" + result));
// 输出：
// 执行了！
// 第2次：结果

// 第3次 subscribe
mono.subscribe(result -> System.out.println("第3次：" + result));
// 输出：
// 执行了！
// 第3次：结果

// 关键：每次 subscribe 都会重新执行！
```

---

## ⚠️ 问题场景

### 场景1：数据库查询被执行多次

```java
@GetMapping("/user/{id}")
public Mono<User> getUser(@PathVariable Long id) {
    Mono<User> mono = userRepository.findById(id);
    
    // ❌ 错误：手动 subscribe
    mono.subscribe(user -> {
        System.out.println("查询到用户：" + user.getName());
    });
    
    // 返回给框架
    return mono;
    
    // 问题：
    // - 手动 subscribe → 查询数据库（第1次）
    // - 框架 subscribe → 查询数据库（第2次）
    // - 数据库负载翻倍！
}
```

### 场景2：副作用被执行多次

```java
@PostMapping("/order")
public Mono<Order> createOrder(@RequestBody OrderRequest request) {
    Mono<Order> mono = orderService.createOrder(request);
    
    // ❌ 错误：手动 subscribe
    mono.subscribe(order -> {
        System.out.println("订单创建成功：" + order.getId());
        // 发送通知
        notificationService.sendNotification(order);
    });
    
    // 返回给框架
    return mono;
    
    // 问题：
    // - 手动 subscribe → 创建订单（第1次）+ 发送通知（第1次）
    // - 框架 subscribe → 创建订单（第2次）+ 发送通知（第2次）
    // - 订单重复创建！通知重复发送！
}
```

### 场景3：计数器被增加多次

```java
private AtomicInteger counter = new AtomicInteger(0);

@GetMapping("/count")
public Mono<Integer> count() {
    Mono<Integer> mono = Mono.fromCallable(() -> {
        int count = counter.incrementAndGet();
        System.out.println("计数：" + count);
        return count;
    });
    
    // ❌ 错误：手动 subscribe
    mono.subscribe(c -> System.out.println("手动 subscribe：" + c));
    
    // 返回给框架
    return mono;
    
    // 问题：
    // - 手动 subscribe → 计数+1（第1次）
    // - 框架 subscribe → 计数+1（第2次）
    // - 计数器增加了2次！
}
```

---

## ✅ 正确做法

### 方法1：不要手动 subscribe（推荐）

```java
@GetMapping("/user/{id}")
public Mono<User> getUser(@PathVariable Long id) {
    // ✅ 正确：只返回 Mono，让框架 subscribe
    return userRepository.findById(id)
            .doOnNext(user -> {
                // 使用 doOnNext 记录日志
                System.out.println("查询到用户：" + user.getName());
            });
    
    // 只会执行一次
}
```

### 方法2：使用 doOnNext/doOnSuccess 代替 subscribe

```java
@GetMapping("/user/{id}")
public Mono<User> getUser(@PathVariable Long id) {
    // ✅ 正确：使用 doOnNext
    return userRepository.findById(id)
            .doOnNext(user -> {
                // 这个回调会在框架 subscribe 时执行
                System.out.println("查询到用户：" + user.getName());
            })
            .doOnSuccess(user -> {
                // 成功时的回调
                System.out.println("查询成功");
            })
            .doOnError(error -> {
                // 错误时的回调
                System.err.println("查询失败：" + error.getMessage());
            });
    
    // 只会执行一次
}
```

### 方法3：如果确实需要副作用，使用 cache()

```java
@GetMapping("/user/{id}")
public Mono<User> getUser(@PathVariable Long id) {
    // ✅ 正确：使用 cache() 缓存结果
    Mono<User> mono = userRepository.findById(id)
            .cache();  // 缓存结果
    
    // 手动 subscribe（会执行查询）
    mono.subscribe(user -> {
        System.out.println("查询到用户：" + user.getName());
    });
    
    // 返回给框架（使用缓存的结果，不会再次查询）
    return mono;
    
    // 只查询一次数据库，但两个 subscribe 都能收到结果
}
```

---

## 🔬 实验验证

### 实验1：验证多次执行

```java
@GetMapping("/test-multiple-subscribe")
public Mono<String> testMultipleSubscribe() {
    AtomicInteger executionCount = new AtomicInteger(0);
    
    Mono<String> mono = Mono.fromCallable(() -> {
        int count = executionCount.incrementAndGet();
        System.out.println(">>> 执行次数：" + count);
        return "结果-" + count;
    });
    
    // 手动 subscribe（第1次）
    mono.subscribe(result -> {
        System.out.println("手动 subscribe 收到：" + result);
    });
    
    // 返回给框架（第2次）
    return mono;
}

// 访问后控制台输出：
// >>> 执行次数：1
// 手动 subscribe 收到：结果-1
// >>> 执行次数：2
// （客户端收到：结果-2）

// 证明：执行了2次！
```

### 实验2：数据库查询次数

```java
@GetMapping("/test-db-query")
public Mono<User> testDbQuery() {
    Mono<User> mono = Mono.fromCallable(() -> {
        System.out.println(">>> 查询数据库");
        // 模拟数据库查询
        return new User(1L, "张三");
    });
    
    // 手动 subscribe
    mono.subscribe(user -> {
        System.out.println("手动收到：" + user.getName());
    });
    
    // 返回给框架
    return mono;
}

// 访问后控制台输出：
// >>> 查询数据库
// 手动收到：张三
// >>> 查询数据库
// （客户端收到：{"id":1,"name":"张三"}）

// 证明：数据库被查询了2次！
```

---

## 📋 对比表格

| 场景 | 执行次数 | 问题 | 推荐做法 |
|------|---------|------|---------|
| **只返回 Mono** | 1次 | ✅ 无问题 | ✅ 推荐 |
| **手动 subscribe + 返回 Mono** | 2次 | ❌ 重复执行 | ❌ 不推荐 |
| **使用 doOnNext + 返回 Mono** | 1次 | ✅ 无问题 | ✅ 推荐 |
| **使用 cache() + 手动 subscribe + 返回** | 1次 | ✅ 无问题 | ⚠️ 特殊场景 |

---

## 🎯 为什么会这样？

### Mono 的设计理念

```java
// Mono 是"冷"流（Cold Stream）
// 每次 subscribe 都会重新执行

Mono<String> mono = Mono.fromCallable(() -> {
    // 这段代码不是执行一次就缓存
    // 而是每次 subscribe 都执行
    return fetchData();
});

// 第1次 subscribe → 执行 fetchData()
// 第2次 subscribe → 再次执行 fetchData()
// 第3次 subscribe → 又执行 fetchData()
```

### 对比："热"流 vs "冷"流

```
冷流（Cold Stream）：
- 每个订阅者都会触发新的执行
- Mono/Flux 默认是冷流
- 类似于"函数"，每次调用都执行

热流（Hot Stream）：
- 数据源独立于订阅者
- 使用 cache() 可以转为类似热流的行为
- 类似于"广播"，数据只产生一次
```

---

## 💡 常见误区

### 误区1："我手动 subscribe 了，应该不影响框架"

```java
// ❌ 错误想法
"我手动 subscribe 只是为了打印日志，
 应该不影响框架的 subscribe"

// ✅ 实际情况
"每次 subscribe 都会触发完整的执行，
 包括数据库查询、HTTP 调用等"
```

### 误区2："Mono 会缓存结果"

```java
// ❌ 错误想法
"Mono 第一次执行后会缓存结果，
 后续 subscribe 直接返回缓存"

// ✅ 实际情况
"Mono 默认不缓存，每次 subscribe 都重新执行，
 除非显式使用 cache()"
```

### 误区3："框架会检测到我已经 subscribe 了"

```java
// ❌ 错误想法
"框架会检测到我手动 subscribe 了，
 就不会再 subscribe"

// ✅ 实际情况
"框架不知道你手动 subscribe 了，
 它会正常 subscribe 你返回的 Mono"
```

---

## 🛠️ 解决方案

### 场景1：只是想打印日志

```java
// ❌ 错误
Mono<User> mono = userRepository.findById(id);
mono.subscribe(user -> System.out.println(user));
return mono;

// ✅ 正确
return userRepository.findById(id)
        .doOnNext(user -> System.out.println(user));
```

### 场景2：需要做一些副作用操作

```java
// ❌ 错误
Mono<Order> mono = orderService.createOrder(request);
mono.subscribe(order -> notificationService.send(order));
return mono;

// ✅ 正确
return orderService.createOrder(request)
        .doOnSuccess(order -> notificationService.send(order));
```

### 场景3：需要在多个地方使用结果

```java
// ❌ 错误
Mono<User> mono = userRepository.findById(id);
mono.subscribe(user -> logService.log(user));
mono.subscribe(user -> cacheService.cache(user));
return mono;  // 数据库查询3次！

// ✅ 正确：使用 cache()
Mono<User> mono = userRepository.findById(id)
        .cache();  // 缓存结果
mono.subscribe(user -> logService.log(user));
mono.subscribe(user -> cacheService.cache(user));
return mono;  // 只查询1次数据库
```

### 场景4：需要异步处理，但不影响响应

```java
// ❌ 错误
Mono<Order> mono = orderService.createOrder(request);
mono.subscribe(order -> sendEmail(order));  // 异步发邮件
return mono;

// ✅ 正确：使用 subscribeOn
return orderService.createOrder(request)
        .doOnSuccess(order -> {
            // 在后台线程异步发邮件
            Mono.fromRunnable(() -> sendEmail(order))
                    .subscribeOn(Schedulers.boundedElastic())
                    .subscribe();
        });
```

---

## 🎨 可视化对比

### 错误的方式

```
创建 Mono
   ↓
手动 subscribe（第1次）
   ↓
执行查询 ────────→ 数据库
   ↓
打印日志
   ↓
返回 Mono
   ↓
框架 subscribe（第2次）
   ↓
执行查询 ────────→ 数据库（又查一次！）
   ↓
写入响应

结果：数据库被查询2次
```

### 正确的方式

```
创建 Mono + doOnNext
   ↓
返回 Mono
   ↓
框架 subscribe
   ↓
执行查询 ────────→ 数据库
   ↓
触发 doOnNext → 打印日志
   ↓
写入响应

结果：数据库只查询1次
```

---

## 📝 最佳实践

### ✅ 推荐做法

```java
@GetMapping("/user/{id}")
public Mono<User> getUser(@PathVariable Long id) {
    return userRepository.findById(id)
            .doOnSubscribe(s -> 
                System.out.println("开始查询用户：" + id))
            .doOnNext(user -> 
                System.out.println("查询到用户：" + user.getName()))
            .doOnSuccess(user -> 
                System.out.println("查询成功"))
            .doOnError(error -> 
                System.err.println("查询失败：" + error.getMessage()))
            .doFinally(signal -> 
                System.out.println("查询结束"));
}
```

### ❌ 避免做法

```java
@GetMapping("/user/{id}")
public Mono<User> getUser(@PathVariable Long id) {
    Mono<User> mono = userRepository.findById(id);
    
    // ❌ 不要这样做
    mono.subscribe(user -> {
        System.out.println("查询到用户：" + user.getName());
    });
    
    return mono;  // 会导致重复执行
}
```

---

## ✅ 总结

### 核心要点

1. **Mono 可以被多次 subscribe**
   ```
   每次 subscribe 都会触发新的执行
   ```

2. **手动 subscribe + 返回 Mono = 执行2次**
   ```
   你的 subscribe + 框架的 subscribe = 2次执行
   ```

3. **使用 doOnNext/doOnSuccess 代替手动 subscribe**
   ```java
   // ❌ 错误
   mono.subscribe(...)
   return mono;
   
   // ✅ 正确
   return mono.doOnNext(...)
   ```

4. **如果必须多次 subscribe，使用 cache()**
   ```java
   Mono<User> mono = repository.findById(id).cache();
   mono.subscribe(...);  // 第1次
   return mono;          // 第2次，但使用缓存
   ```

---

## 🎯 记忆口诀

```
Mono 可多次订阅，
每次订阅都执行。

手动订阅加框架，
执行两次要注意。

想要副作用处理，
doOnNext 是正道。

必须多次订阅时，
cache 缓存来帮忙。
```

---

## 📚 延伸阅读

- **[WebFlux自动订阅机制.md](./WebFlux自动订阅机制.md)** - subscribe 机制详解
- **[subscribe机制-快速理解.md](./subscribe机制-快速理解.md)** - 快速理解

---

现在你知道了：**手动 subscribe + 返回 Mono = 执行2次！应该使用 doOnNext 代替！** 🎯

