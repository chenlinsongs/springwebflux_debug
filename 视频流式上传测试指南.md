# 视频流式上传测试指南

## 所有可用的视频上传接口

### 1. 标准视频上传

```bash
curl -X POST http://localhost:8080/image/upload-video \
  -F 'file=@video.mp4'
```

**特点**：简单，但未优化

### 2. iOS优化版（推荐给iOS）⭐

```bash
curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@video.mp4'
```

**特点**：
- ✅ 使用transferTo优化
- ✅ 提供详细性能统计
- ✅ iOS完全兼容

### 3. Raw Binary流式上传（真正流式）🚀

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4
```

**特点**：
- ✅ 真正的流式上传
- ✅ 数据到达立即写入
- ✅ 无临时文件

## 快速测试

### 创建测试文件

```bash
# 小文件（10MB）
dd if=/dev/zero of=small.mp4 bs=1m count=10

# 中等文件（50MB）
dd if=/dev/zero of=medium.mp4 bs=1m count=50

# 大文件（500MB）
dd if=/dev/zero of=large.mp4 bs=1m count=500

# 超大文件（1.5GB）
dd if=/dev/zero of=huge.mp4 bs=1m count=1500
```

### 测试iOS优化版

```bash
time curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@medium.mp4' \
  --limit-rate 5M
```

**期望日志**：
```log
⏰ [+10000ms] FilePart到达（文件已完全上传）
⏰ 时间统计:
  总耗时: 10.50 秒
  上传等待: 10.00 秒
  复制耗时: 0.50 秒
  复制占比: 4.8%

✅ 性能良好
```

### 测试Raw Binary

```bash
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: medium.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @medium.mp4 \
  --limit-rate 5M
```

**期望日志**：
```log
📦 接收到视频上传请求 | 文件名: medium.mp4
💾 视频已刷新到磁盘 | 数据块: 500 | 磁盘大小: 2.50 MB | 速度: 5.0 MB/s
💾 视频已刷新到磁盘 | 数据块: 1000 | 磁盘大小: 5.00 MB | 速度: 5.0 MB/s
...
✅ 视频最终刷新完成，总数据块: 2500
```

## 验证真正的流式

### 方法1: 监控文件大小

```bash
# 终端1: 上传文件
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @medium.mp4 \
  --limit-rate 5M

# 终端2: 监控文件大小变化
while true; do 
  ls -lh ~/Movies/uploads/test.mp4 2>/dev/null || echo "等待文件创建..."
  sleep 1
done
```

**Raw Binary 期望输出**：
```
等待文件创建...
-rw-r--r--  1 user  staff   2.5M Nov 22 20:30 test.mp4
-rw-r--r--  1 user  staff   5.0M Nov 22 20:31 test.mp4
-rw-r--r--  1 user  staff   7.5M Nov 22 20:32 test.mp4
...
```

**Multipart 输出对比**：
```
等待文件创建...
等待文件创建...
等待文件创建...
... (等待10秒)
-rw-r--r--  1 user  staff  50.0M Nov 22 20:40 test.mp4  ← 一次性出现！
```

### 方法2: 观察日志时间

```bash
# 查看日志
tail -f logs/spring.log | grep -E "视频|FilePart"
```

**Raw Binary 日志**：
```
[20:30:00.050] 📦 接收到视频上传请求  ← 上传开始后50ms
[20:30:01.000] 💾 视频已刷新到磁盘
[20:30:02.000] 💾 视频已刷新到磁盘
...
```

**Multipart 日志**：
```
... (无日志，等待中) ...
[20:30:10.000] ⏰ FilePart到达  ← 上传完成后才有日志
[20:30:10.500] ⏰ 时间统计
```

## 性能对比测试

### 测试脚本

```bash
#!/bin/bash

echo "🧪 视频上传性能对比测试"
echo ""

# 创建测试文件
dd if=/dev/zero of=test.mp4 bs=1m count=50 2>/dev/null

echo "1️⃣ 测试 Multipart (标准)"
multipart_start=$(date +%s.%N)
curl -s -X POST http://localhost:8080/image/upload-video \
  -F 'file=@test.mp4' > /dev/null
multipart_end=$(date +%s.%N)
multipart_time=$(echo "$multipart_end - $multipart_start" | bc)

echo "2️⃣ 测试 Multipart (iOS优化)"
optimized_start=$(date +%s.%N)
curl -s -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@test.mp4' > /dev/null
optimized_end=$(date +%s.%N)
optimized_time=$(echo "$optimized_end - $optimized_start" | bc)

echo "3️⃣ 测试 Raw Binary"
raw_start=$(date +%s.%N)
curl -s -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  --data-binary @test.mp4 > /dev/null
raw_end=$(date +%s.%N)
raw_time=$(echo "$raw_end - $raw_start" | bc)

echo ""
echo "📊 性能对比结果:"
echo "  Multipart (标准):   ${multipart_time}s"
echo "  Multipart (iOS优化): ${optimized_time}s"
echo "  Raw Binary:         ${raw_time}s"

# 清理
rm test.mp4

echo ""
echo "✅ 测试完成"
```

## iOS Swift 测试代码

### 测试Multipart上传

```swift
func testMultipartUpload() {
    let videoURL = URL(fileURLWithPath: "/path/to/video.mp4")
    
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-ios-optimized")!)
    request.httpMethod = "POST"
    
    let boundary = UUID().uuidString
    request.setValue("multipart/form-data; boundary=\(boundary)", forHTTPHeaderField: "Content-Type")
    
    let videoData = try! Data(contentsOf: videoURL)
    var body = Data()
    body.append("--\(boundary)\r\n")
    body.append("Content-Disposition: form-data; name=\"file\"; filename=\"video.mp4\"\r\n")
    body.append("Content-Type: video/mp4\r\n\r\n")
    body.append(videoData)
    body.append("\r\n--\(boundary)--\r\n")
    
    request.httpBody = body
    
    let startTime = Date()
    URLSession.shared.dataTask(with: request) { data, response, error in
        let elapsed = Date().timeIntervalSince(startTime)
        print("✅ Multipart 上传完成，耗时: \(elapsed)秒")
    }.resume()
}
```

### 测试Raw Binary上传

```swift
func testRawBinaryUpload() {
    let videoURL = URL(fileURLWithPath: "/path/to/video.mp4")
    
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-raw")!)
    request.httpMethod = "POST"
    request.setValue("application/octet-stream", forHTTPHeaderField: "Content-Type")
    request.setValue("video.mp4", forHTTPHeaderField: "X-Filename")
    request.setValue("500", forHTTPHeaderField: "X-Force-Interval")
    
    request.httpBodyStream = InputStream(url: videoURL)
    
    let startTime = Date()
    URLSession.shared.uploadTask(with: request, fromFile: videoURL) { data, response, error in
        let elapsed = Date().timeIntervalSince(startTime)
        print("✅ Raw Binary 上传完成，耗时: \(elapsed)秒")
    }.resume()
}
```

## 清理测试环境

```bash
# 清理测试文件
rm small.mp4 medium.mp4 large.mp4 huge.mp4

# 清理上传的文件
rm ~/Movies/uploads/*.mp4

# 清理临时文件
rm /tmp/spring-multipart-*

# 检查磁盘空间
df -h
```

## 故障排查

查看 `视频流式上传-故障排查指南.md`

## 下一步

- 理解原理 → `FilePart流式上传原理.md`
- 选择方案 → `视频上传方案总结.md`
- iOS方案 → `iOS_Multipart上传的现实方案.md`

