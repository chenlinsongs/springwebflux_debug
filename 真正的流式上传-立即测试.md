# 真正的流式上传 - 立即测试

## 🎯 核心接口

`/upload-video-raw` - 真正的流式上传，数据到达立即写入磁盘

## ✅ 立即测试

### 1. 创建测试文件

```bash
# 创建 50MB 测试文件
dd if=/dev/zero of=test_video.mp4 bs=1m count=50
```

### 2. 上传测试

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test_video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test_video.mp4 \
  --limit-rate 5M \
  -v
```

**参数说明**：
- `Content-Type: application/octet-stream` - 原始二进制流
- `X-Filename: test_video.mp4` - 文件名（必需）
- `X-Force-Interval: 500` - 每500个数据块强制刷新到磁盘（可选）
- `--limit-rate 5M` - 限速5MB/s，方便观察流式效果

### 3. 观察日志

在另一个终端：

```bash
tail -f logs/spring.log | grep "视频"
```

**期望看到**：

```log
📦 接收到视频上传请求 | 文件名: test_video.mp4
💾 视频已刷新到磁盘 | 数据块: 500 | 磁盘大小: 2.50 MB | 速度: 5.0 MB/s
💾 视频已刷新到磁盘 | 数据块: 1000 | 磁盘大小: 5.00 MB | 速度: 5.0 MB/s
💾 视频已刷新到磁盘 | 数据块: 1500 | 磁盘大小: 7.50 MB | 速度: 5.0 MB/s
...
✅ 视频最终刷新完成，总数据块: 2500
```

**关键观察**：
- ✅ 上传开始后 **50ms内** 看到第一条"视频已刷新到磁盘"日志
- ✅ 日志持续输出，每500个数据块输出一次
- ✅ 实时显示当前磁盘大小和速度

### 4. 验证文件立即写入

在上传过程中，在第三个终端：

```bash
# 监控目标文件大小变化
while true; do 
  ls -lh ~/Movies/uploads/test_video.mp4 2>/dev/null || echo "文件还未创建"
  sleep 1
done
```

**期望看到**：

```
-rw-r--r--  1 user  staff   2.5M Nov 22 20:30 test_video.mp4
-rw-r--r--  1 user  staff   5.0M Nov 22 20:31 test_video.mp4
-rw-r--r--  1 user  staff   7.5M Nov 22 20:32 test_video.mp4
...
-rw-r--r--  1 user  staff  50.0M Nov 22 20:40 test_video.mp4
```

**关键观察**：
- ✅ 文件在上传开始后 **立即创建**
- ✅ 文件大小 **持续增长**
- ✅ 这才是真正的流式上传！

## 📊 对比测试

### Multipart 方式

```bash
curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
  -F 'file=@test_video.mp4' \
  --limit-rate 5M
```

**观察**：
```
[0-10s]  上传中，服务端无日志
[10s]    iOS显示上传完成
          ↓
[10.1s]  服务端才开始打印日志  ← 延迟！
[11s]    服务端处理完成
```

**文件大小监控**：
```
文件还未创建
文件还未创建
文件还未创建
... (等待10秒)
-rw-r--r--  1 user  staff  50.0M Nov 22 20:40 test_video.mp4  ← 一次性写入！
```

### Raw Binary 方式（本方案）

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test_video.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test_video.mp4 \
  --limit-rate 5M
```

**观察**：
```
[0.05s]  服务端开始打印日志  ← 立即开始！
[1s]     💾 视频已刷新到磁盘 | 数据块: 500 | 磁盘大小: 2.50 MB
[2s]     💾 视频已刷新到磁盘 | 数据块: 1000 | 磁盘大小: 5.00 MB
...
[10s]    ✅ 视频最终刷新完成
```

**文件大小监控**：
```
-rw-r--r--  1 user  staff   0B   Nov 22 20:30 test_video.mp4  ← 立即创建
-rw-r--r--  1 user  staff   2.5M Nov 22 20:31 test_video.mp4  ← 持续增长
-rw-r--r--  1 user  staff   5.0M Nov 22 20:32 test_video.mp4
...
-rw-r--r--  1 user  staff  50.0M Nov 22 20:40 test_video.mp4
```

## 🎯 核心差异

| 特性 | Multipart | Raw Binary |
|-----|----------|------------|
| 日志开始时间 | 上传完成后 | 上传开始后50ms |
| 文件创建时间 | 上传完成后 | 上传开始后50ms |
| 文件大小变化 | 一次性写入 | 持续增长 ✅ |
| 真正流式 | ❌ | ✅ |

## 🚀 iOS 客户端示例

```swift
func uploadVideoRaw(url: URL, filename: String) {
    var request = URLRequest(url: URL(string: "http://server/image/upload-video-raw")!)
    request.httpMethod = "POST"
    
    // 关键配置
    request.setValue("application/octet-stream", forHTTPHeaderField: "Content-Type")
    request.setValue(filename, forHTTPHeaderField: "X-Filename")
    request.setValue("video/mp4", forHTTPHeaderField: "X-Content-Type")
    request.setValue("500", forHTTPHeaderField: "X-Force-Interval")
    
    // 使用InputStream实现真正的流式上传
    request.httpBodyStream = InputStream(url: url)
    
    let task = URLSession.shared.uploadTask(with: request, fromFile: url) { data, response, error in
        if let error = error {
            print("上传失败: \(error)")
            return
        }
        
        if let data = data, let result = try? JSONDecoder().decode([String: Any].self, from: data) {
            print("上传成功: \(result)")
        }
    }
    
    task.resume()
}
```

## ⚡ 性能参数调优

### forceInterval 参数

```bash
# 不强制刷新（最快，但可能丢失数据）
-H "X-Force-Interval: 0"

# 每100个数据块强制刷新（高频率，更安全）
-H "X-Force-Interval: 100"

# 每500个数据块强制刷新（推荐，平衡性能和安全）
-H "X-Force-Interval: 500"

# 每1000个数据块强制刷新（低频率，更快但风险稍高）
-H "X-Force-Interval: 1000"
```

### 性能测试

```bash
# 测试不同刷新频率的性能

# 无刷新
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 0" \
  --data-binary @test_video.mp4

# 高频刷新
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 100" \
  --data-binary @test_video.mp4

# 推荐频率
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @test_video.mp4
```

## 🎉 成功标志

如果看到以下现象，说明真正的流式上传已经工作：

1. ✅ 上传开始后50ms内看到日志
2. ✅ 目标文件立即创建
3. ✅ 文件大小持续增长
4. ✅ 日志显示实时速度和进度
5. ✅ 服务器内存占用保持稳定

## 下一步

- 查看 `视频上传方案总结.md` 了解所有方案对比
- 查看 `WebFlux_Multipart限制说明.md` 理解Multipart的限制
- 调整 `forceInterval` 参数优化性能

**现在就可以开始测试真正的流式上传了！** 🚀

