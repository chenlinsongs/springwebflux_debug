# 验证真正的流式上传

## 验证目标

确认服务端是否实现了真正的流式上传，即：
- ✅ 数据到达立即写入
- ✅ 文件立即创建
- ✅ 文件大小持续增长
- ✅ 内存占用恒定

## 验证方法

### 方法1: 监控文件大小变化

#### 步骤

**终端1：启动上传**

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4 \
  --limit-rate 5M
```

**终端2：监控文件大小**

```bash
while true; do 
  ls -lh ~/Movies/uploads/test.mp4 2>/dev/null || echo "⏳ 等待文件创建..."
  sleep 1
done
```

#### 期望结果（真正流式）

```
⏳ 等待文件创建...
-rw-r--r--  1 user  staff   0B   Nov 22 20:30 test.mp4
-rw-r--r--  1 user  staff   2.5M Nov 22 20:31 test.mp4  ← 持续增长
-rw-r--r--  1 user  staff   5.0M Nov 22 20:32 test.mp4
-rw-r--r--  1 user  staff   7.5M Nov 22 20:33 test.mp4
-rw-r--r--  1 user  staff  10.0M Nov 22 20:34 test.mp4
...
```

**验证通过标志**：
- ✅ 文件在上传开始后1秒内创建
- ✅ 文件大小每秒增加约5MB（与限速一致）
- ✅ 文件大小持续、平滑增长

#### 错误示例（非流式）

```
⏳ 等待文件创建...
⏳ 等待文件创建...
⏳ 等待文件创建...
... (等待10秒) ...
-rw-r--r--  1 user  staff  50.0M Nov 22 20:40 test.mp4  ← 一次性出现！
```

**问题**：
- ❌ 等待时间过长
- ❌ 文件一次性出现，不是持续增长
- ❌ 这不是流式上传！

### 方法2: 观察日志时间

#### 步骤

**终端1：查看日志**

```bash
tail -f logs/spring.log | grep -E "视频|数据块"
```

**终端2：启动上传**

```bash
time curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @video.mp4 \
  --limit-rate 5M
```

#### 期望结果（真正流式）

```log
[20:30:00.050] 📦 接收到视频上传请求 | 文件名: test.mp4  ← 立即开始
[20:30:01.000] 💾 视频已刷新到磁盘 | 数据块: 500 | 磁盘大小: 2.50 MB
[20:30:02.000] 💾 视频已刷新到磁盘 | 数据块: 1000 | 磁盘大小: 5.00 MB
[20:30:03.000] 💾 视频已刷新到磁盘 | 数据块: 1500 | 磁盘大小: 7.50 MB
...
[20:30:10.000] ✅ 视频最终刷新完成，总数据块: 2500
```

**验证通过标志**：
- ✅ 第一条日志在上传开始后50ms内出现
- ✅ 日志持续、定期输出（每秒一次）
- ✅ 显示实时进度和速度

#### 错误示例（非流式）

```log
... (无任何日志，等待10秒) ...
[20:30:10.000] ⏰ FilePart到达（文件已完全上传）  ← 延迟！
[20:30:10.500] ⏰ 时间统计: 总耗时10.5秒
```

**问题**：
- ❌ 没有实时日志
- ❌ 所有日志在上传完成后才出现
- ❌ 这不是流式上传！

### 方法3: 监控内存占用

#### 步骤

**终端1：监控Java进程内存**

```bash
# macOS
top -pid $(pgrep -f spring) -stats mem

# Linux
top -p $(pgrep -f java) -d 1
```

**终端2：上传大文件**

```bash
# 创建1GB测试文件
dd if=/dev/zero of=big.mp4 bs=1m count=1000

# 上传
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: big.mp4" \
  -H "X-Force-Interval: 500" \
  --data-binary @big.mp4
```

#### 期望结果（真正流式）

```
时间    内存占用
0s      500MB   (基线)
10s     520MB   (+20MB)  ← 仅增加缓冲区大小
20s     525MB   (+25MB)
30s     530MB   (+30MB)
40s     520MB   (+20MB)  ← 波动很小
...
```

**验证通过标志**：
- ✅ 内存增长很小（<50MB）
- ✅ 内存占用稳定
- ✅ 不会随文件大小线性增长

#### 错误示例（非流式）

```
时间    内存占用
0s      500MB   (基线)
10s     600MB   (+100MB)
20s     700MB   (+200MB)  ← 持续增长
30s     800MB   (+300MB)
40s     900MB   (+400MB)  ← 接近文件大小
```

**问题**：
- ❌ 内存持续增长
- ❌ 增长量接近文件大小
- ❌ 文件被缓冲到内存！

### 方法4: 使用网络抓包

#### 步骤

**启动Wireshark或tcpdump**

```bash
sudo tcpdump -i lo0 -w upload.pcap 'port 8080'
```

**上传文件**

```bash
curl -X POST http://localhost:8080/image/upload-video-raw \
  -H "Content-Type: application/octet-stream" \
  -H "X-Filename: test.mp4" \
  --data-binary @video.mp4 \
  --limit-rate 5M
```

**分析抓包文件**

```bash
# 查看HTTP流
wireshark upload.pcap
# Follow -> HTTP Stream
```

#### 期望观察

1. **HTTP请求头立即发送**
```
POST /image/upload-video-raw HTTP/1.1
Content-Type: application/octet-stream
X-Filename: test.mp4
Content-Length: 52428800
```

2. **请求体分批发送**
```
[数据块1] 8KB
[数据块2] 8KB
[数据块3] 8KB
... (持续发送)
```

3. **服务端立即开始处理**
   - 文件立即创建
   - 无需等待完整body

### 方法5: 压力测试

#### 创建测试脚本

```bash
#!/bin/bash

echo "🧪 流式上传压力测试"

# 创建测试文件
dd if=/dev/zero of=test.mp4 bs=1m count=100 2>/dev/null

# 并发10个上传
for i in {1..10}; do
  {
    echo "[$i] 开始上传..."
    start=$(date +%s)
    
    curl -s -X POST http://localhost:8080/image/upload-video-raw \
      -H "Content-Type: application/octet-stream" \
      -H "X-Filename: test_${i}.mp4" \
      -H "X-Force-Interval: 500" \
      --data-binary @test.mp4 > /dev/null
    
    end=$(date +%s)
    echo "[$i] 完成，耗时: $((end - start))秒"
  } &
done

# 监控内存
{
  echo "监控内存占用..."
  for t in {1..30}; do
    mem=$(ps -o rss= -p $(pgrep -f spring) | awk '{print $1/1024}')
    echo "[${t}s] 内存: ${mem}MB"
    sleep 1
  done
} &

wait

echo "✅ 测试完成"

# 清理
rm test.mp4
```

#### 期望结果

```
🧪 流式上传压力测试
[1] 开始上传...
[2] 开始上传...
...
[10] 开始上传...

监控内存占用...
[1s] 内存: 520MB
[2s] 内存: 540MB
[3s] 内存: 560MB
...
[30s] 内存: 580MB  ← 增长有限

[1] 完成，耗时: 25秒
[2] 完成，耗时: 26秒
...
[10] 完成，耗时: 28秒

✅ 测试完成
```

**验证通过标志**：
- ✅ 并发10个100MB文件上传
- ✅ 内存增长<100MB（不是1GB）
- ✅ 所有上传成功完成

## 对比验证

### 同时测试两种方式

```bash
#!/bin/bash

echo "=== 对比测试：Multipart vs Raw Binary ==="

# 创建测试文件
dd if=/dev/zero of=test.mp4 bs=1m count=50 2>/dev/null

echo ""
echo "1️⃣ 测试 Multipart 上传..."
echo "   （观察文件创建时间）"

# 终端1
{
  curl -X POST http://localhost:8080/image/upload-video-ios-optimized \
    -F 'file=@test.mp4' \
    --limit-rate 5M
} &

# 终端2
{
  sleep 1
  while true; do 
    if [ -f ~/Movies/uploads/video_*.mp4 ]; then
      ls -lh ~/Movies/uploads/video_*.mp4 | tail -1
      break
    fi
    echo "⏳ Multipart: 等待文件创建... ($(date +%S)秒)"
    sleep 1
  done
} &

wait

echo ""
echo "2️⃣ 测试 Raw Binary 上传..."
echo "   （观察文件创建时间）"

# 终端1
{
  curl -X POST http://localhost:8080/image/upload-video-raw \
    -H "Content-Type: application/octet-stream" \
    -H "X-Filename: test_raw.mp4" \
    -H "X-Force-Interval: 500" \
    --data-binary @test.mp4 \
    --limit-rate 5M
} &

# 终端2
{
  sleep 0.1
  while true; do 
    if [ -f ~/Movies/uploads/test_raw.mp4 ]; then
      ls -lh ~/Movies/uploads/test_raw.mp4
      echo "✅ Raw Binary: 文件立即创建！"
      break
    fi
    echo "⏳ Raw Binary: 等待文件创建... ($(date +%S)秒)"
    sleep 0.5
  done
} &

wait

echo ""
echo "✅ 对比测试完成"
echo ""
echo "预期结果："
echo "  - Multipart: 等待10秒后文件一次性出现"
echo "  - Raw Binary: 0.5秒内文件创建，持续增长"
```

## 验证清单

### 真正的流式上传应该满足

- [ ] 文件在上传开始后1秒内创建
- [ ] 文件大小持续、平滑增长
- [ ] 日志实时输出（不是上传完成后才输出）
- [ ] 第一条日志在50ms内出现
- [ ] 内存占用恒定（<50MB增长）
- [ ] 支持超大文件（>5GB）不会OOM
- [ ] 网络数据分批到达，立即处理
- [ ] 并发上传时内存增长有限

### 如果以下情况，说明不是流式

- [ ] 等待10秒以上才看到日志
- [ ] 文件一次性出现，不是持续增长
- [ ] 内存占用接近文件大小
- [ ] 日志在上传完成后批量输出
- [ ] 文件创建延迟超过5秒

## 故障排查

### 问题1: 文件没有立即创建

**可能原因**：
1. 使用了错误的接口（Multipart）
2. forceInterval设置为0（不刷新）
3. 文件路径没有权限

**解决**：
```bash
# 确认使用Raw Binary接口
curl ... -H "Content-Type: application/octet-stream" ...

# 设置合理的forceInterval
-H "X-Force-Interval: 500"

# 检查目录权限
ls -ld ~/Movies/uploads/
mkdir -p ~/Movies/uploads/
chmod 755 ~/Movies/uploads/
```

### 问题2: 看不到实时日志

**可能原因**：
1. 日志级别不正确
2. 日志文件位置错误
3. forceInterval为null

**解决**：
```yaml
# application.yml
logging:
  level:
    org.example.springwebflux.controller: DEBUG
  file:
    name: logs/spring.log
```

### 问题3: 内存持续增长

**可能原因**：
1. DataBuffer没有释放
2. 使用了错误的调度器

**解决**：
```java
// 确保释放
DataBufferUtils.release(dataBuffer);

// 使用正确的调度器
.publishOn(Schedulers.boundedElastic())
```

## 成功案例

### 验证成功的输出示例

```bash
$ ./verify_streaming.sh

🧪 验证真正的流式上传

1️⃣ 启动上传（限速5MB/s）...
2️⃣ 监控文件大小...

⏳ 等待文件创建...
✅ [0.05s] 文件已创建！
📊 [1s] 大小: 5.0MB
📊 [2s] 大小: 10.0MB
📊 [3s] 大小: 15.0MB
📊 [4s] 大小: 20.0MB
📊 [5s] 大小: 25.0MB
...
📊 [10s] 大小: 50.0MB
✅ 上传完成！

验证结果:
  ✅ 文件创建延迟: 50ms
  ✅ 文件持续增长: 是
  ✅ 增长速度: 5.0MB/s （符合限速）
  ✅ 内存增长: 15MB （正常）

🎉 验证通过！这是真正的流式上传！
```

## 总结

### 验证真正流式上传的黄金标准

1. **文件立即创建**（<1秒）
2. **大小持续增长**（不是一次性）
3. **日志实时输出**（<50ms）
4. **内存占用恒定**（<50MB增长）

### 推荐验证方法

**最简单**：方法1（监控文件大小）
**最直观**：方法2（观察日志时间）
**最准确**：方法3（监控内存）

现在您可以自信地验证流式上传了！✅

