# Spring WebFlux 测试指南

## 快速开始

我已经为你创建了以下文件：

1. **WebFluxExampleController.java** - 完整的代码示例和详细注释
2. **PerformanceComparisonController.java** - 可以直接测试的性能对比接口
3. **WebFlux_vs_Servlet_说明.md** - 详细的技术文档
4. **快速理解WebFlux.md** - 5分钟快速入门

---

## 立即测试（最重要！）

### 步骤1：启动你的应用
```bash
cd /Users/linsong.chen/IdeaProjects/my/springwebflux_debug
mvn spring-boot:run
```

### 步骤2：访问测试接口

打开浏览器或使用 `curl` 命令：

#### 🌟 推荐先测试这个（最能体现WebFlux优势）
```bash
# 并行调用3个服务，只需要200ms（而不是600ms）
curl http://localhost:8080/comparison/webflux-parallel
```

**期望输出**：
```
【并行】结果：数据-1, 数据-2, 数据-3 | 总耗时：200ms（节省了约400ms！）
```

**对比**：串行调用
```bash
curl http://localhost:8080/comparison/webflux-sequential
```
**期望输出**：
```
【串行】结果：数据-4, 数据-5, 数据-6 | 总耗时：600ms
```

---

#### 其他测试接口

1. **查看所有接口说明**
```bash
curl http://localhost:8080/comparison/summary
```

2. **单个异步请求**
```bash
curl http://localhost:8080/comparison/webflux-single
```

3. **流式数据推送（实时）**
```bash
curl http://localhost:8080/comparison/stream
```
这个命令会持续10秒，每秒输出一条数据

4. **错误处理演示**
```bash
curl http://localhost:8080/comparison/error-handling
```

5. **线程使用情况**
```bash
curl http://localhost:8080/comparison/thread-info
```

---

## 观察要点

### 1. 查看控制台日志

运行测试时，观察控制台输出，你会看到：

```
【WebFlux】开始处理请求，线程：reactor-http-nio-2
[请求 1] 模拟服务开始处理，线程：reactor-http-nio-2
[请求 1] 模拟服务完成，线程：parallel-1
【WebFlux】结果：数据-1 | 耗时：202ms | 开始线程：reactor-http-nio-2 | 结束线程：parallel-1
```

**注意**：
- 开始和结束可能在不同的线程！
- 线程名称包含 `reactor` 或 `parallel`，说明使用了少量线程
- 这就是异步非阻塞的证明

---

### 2. 并行 vs 串行的时间差异

**串行调用**（传统方式）：
```
调用1（200ms）→ 调用2（200ms）→ 调用3（200ms）= 600ms
```

**并行调用**（WebFlux优势）：
```
调用1（200ms）┐
调用2（200ms）├→ max(200,200,200) = 200ms
调用3（200ms）┘
```

**节省时间：400ms（66%提升！）**

---

## 实战场景模拟

### 场景1：查询用户详情（需要调用3个服务）

传统Servlet方式：
```java
// 需要 300ms + 200ms + 150ms = 650ms
User user = restTemplate.getForObject("/users/1");        // 300ms
List<Order> orders = restTemplate.getForObject("/orders"); // 200ms
Address address = restTemplate.getForObject("/addresses"); // 150ms
```

WebFlux方式（并行）：
```java
// 只需要 max(300, 200, 150) = 300ms
Mono<User> userMono = webClient.get().uri("/users/1").retrieve().bodyToMono(User.class);
Mono<List<Order>> ordersMono = webClient.get().uri("/orders").retrieve().bodyToFlux(Order.class).collectList();
Mono<Address> addressMono = webClient.get().uri("/addresses").retrieve().bodyToMono(Address.class);

return Mono.zip(userMono, ordersMono, addressMono).map(...);
```

**时间节省：54%**

---

## 常见问题

### Q1：为什么我的代码看起来更复杂了？

A：是的，响应式代码的学习曲线较陡。但这是值得的：
- 更高的吞吐量（2-3倍）
- 更少的资源消耗（10倍以下的线程数）
- 支持流式处理和实时推送

### Q2：什么时候应该用WebFlux？

A：以下场景推荐使用：
- ✅ 高并发场景（>1000 QPS）
- ✅ 需要调用多个外部服务
- ✅ IO密集型应用（大量数据库/HTTP调用）
- ✅ 需要实时推送（WebSocket、SSE）

### Q3：什么时候不应该用WebFlux？

A：以下场景不推荐：
- ❌ CPU密集型计算
- ❌ 使用阻塞的第三方库（如JDBC，需要改用R2DBC）
- ❌ 团队对响应式编程不熟悉
- ❌ 简单的CRUD应用

### Q4：我能混合使用吗？

A：可以！
- 简单接口用传统方式
- 高并发接口用WebFlux
- 同一个应用中可以混合使用

---

## 核心要点（必须记住）

### 1. Mono 和 Flux 是"承诺"

```java
Mono<String> mono = Mono.just("Hello");
// 这不是String，这是"将来会有一个String"的承诺
```

### 2. 方法立即返回

```java
@GetMapping("/test")
public Mono<String> test() {
    return webClient.get().uri("/api").retrieve().bodyToMono(String.class);
    // 这一行会立即返回，不会等待HTTP响应！
}
```

### 3. 不要阻塞线程

```java
// ❌ 错误
return Mono.fromCallable(() -> {
    Thread.sleep(1000);  // 阻塞了EventLoop线程！
    return "Result";
});

// ✅ 正确
return Mono.fromCallable(() -> {
    Thread.sleep(1000);
    return "Result";
}).subscribeOn(Schedulers.boundedElastic());  // 在专门的线程池执行
```

### 4. 使用 WebClient 而不是 RestTemplate

```java
// ❌ 阻塞
RestTemplate restTemplate = new RestTemplate();
String result = restTemplate.getForObject(...);

// ✅ 非阻塞
WebClient webClient = WebClient.create();
Mono<String> result = webClient.get().uri(...).retrieve().bodyToMono(String.class);
```

---

## 下一步

1. ✅ 先运行测试接口，观察实际效果
2. ✅ 阅读 `快速理解WebFlux.md`（5分钟）
3. ✅ 查看 `WebFluxExampleController.java` 的代码示例
4. ✅ 深入学习 `WebFlux_vs_Servlet_说明.md`

---

## 总结

**一句话总结**：
> WebFlux 不是让单个请求变快，而是让系统能用更少的资源处理更多的并发请求。

**关键数字**：
- 传统Servlet：1000个并发 = 1000个线程 = 1GB内存
- Spring WebFlux：1000个并发 = 8-16个线程 = 16MB内存

**核心思想**：
- 不要等待，立即返回
- 数据准备好时，通过回调处理
- 少量线程处理大量请求

---

现在就开始测试吧！运行 `curl http://localhost:8080/comparison/webflux-parallel` 感受并行调用的威力！🚀

